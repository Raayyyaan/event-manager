# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=votre_mot_de_passe_postgresql
DB_NAME=event_manager

# JWT Secret (générez une clé aléatoire sécurisée)
JWT_SECRET=votre_secret_jwt_tres_long_et_securise_changez_moi

# Server Port
PORT=5000

# Node Environment
NODE_ENV=development
```

**Contenu de `.gitkeep` :**
```
# Ce fichier permet de garder le dossier uploads/ dans git
# sans commiter les images uploadées par les utilisateurs